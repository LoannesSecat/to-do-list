name: Deploy

on:
  push:
    branches: [master]

jobs:
  build-and-deploy:
    runs-on: ubuntu-20.04
    steps:
      - name: Install and build
        run: |
          npm install
          npm run build
          npm run deploy
